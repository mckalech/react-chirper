var css = require("../styles/styles.scss"); 
var ReactDOM = require('react-dom'),
	React = require('react'),
	Box = require('./box');


ReactDOM.render(
	(
		<Box/>
	),
	document.getElementById('content')
);
